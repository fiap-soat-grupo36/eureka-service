apiVersion: v1
kind: Service
metadata:
  name: eureka-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: eureka-service
  ports:
    - name: http
      port: 8761
      targetPort: http
